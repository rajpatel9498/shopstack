apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
  namespace: shopstack
  labels:
    app: catalog-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: catalog-service
  template:
    metadata:
      labels:
        app: catalog-service
    spec:
      containers:
      - name: catalog
        image: shopstack_catalog:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8001
        env:
        - name: OTLP_ENDPOINT
          valueFrom:
            configMapKeyRef:
              name: shopstack-config
              key: OTLP_ENDPOINT
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8001
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: shopstack
spec:
  selector:
    app: catalog-service
  ports:
  - port: 8001
    targetPort: 8001
  type: ClusterIP
